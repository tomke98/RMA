/**
 * Report feature: the lost/found create flow and the report detail + phone
 * reveal (AD-6, AD-7, AD-8, AD-16). Populated in Epic 2 ({@code CreateReportActivity},
 * {@code ReportViewModel}) and Epic 3 ({@code ReportDetailActivity}). Empty at
 * bootstrap; the package is part of the fixed skeleton.
 */
package hr.ferit.lostandfound.ui.report;
