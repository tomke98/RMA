/**
 * Local notifications: the client-side category alert computed by the Board
 * listener (AD-11). Populated in Story 4.2 ({@code CategoryNotifier},
 * {@code NotificationChannels}).
 */
package hr.ferit.lostandfound.notify;
