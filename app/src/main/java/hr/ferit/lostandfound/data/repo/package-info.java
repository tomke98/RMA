/**
 * Repository layer: the single source of truth per area (AD-1, AD-17). Populated
 * in Stories 1.2-1.3 and Epics 2-3 ({@code AuthRepository}, {@code ReportRepository},
 * {@code BoardRepository}). Empty at bootstrap; the package is part of the fixed
 * skeleton.
 */
package hr.ferit.lostandfound.data.repo;
