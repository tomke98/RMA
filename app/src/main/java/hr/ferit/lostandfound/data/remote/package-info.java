/**
 * Data-source layer: Firestore / Auth / Storage calls (AD-1). Populated in
 * Stories 1.2-1.3 and Epics 2-3 ({@code AuthSource}, {@code FirestoreReportSource},
 * {@code PhotoStorageSource}). Empty at bootstrap; the package is part of the
 * fixed skeleton.
 */
package hr.ferit.lostandfound.data.remote;
